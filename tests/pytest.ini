[pytest]
env =
    # When running `pytest` *inside* the api-test container, the API is on 8080.
    CAD_AGENT_BASE_URL=http://localhost:8080
markers =
    integration: requires running docker services (api on localhost)

