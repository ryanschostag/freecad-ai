[pytest]
env =
    CAD_AGENT_BASE_URL=http://localhost:8081
markers =
    integration: requires running docker services (api on localhost)
pythonpath =
    services/api
    services/freecad-worker
filterwarnings =
    ignore:datetime\.datetime\.utcnow\(\) is deprecated.*:DeprecationWarning:rq\..*
